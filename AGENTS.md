# AGENTS.md

すべての思考・回答・コメントは日本語で行うこと。
利用者への報告、途中経過、最終回答も必ず日本語にする。
英語の指示書やツール出力を読んだ直後も英語へ切り替えない。

Vesselbound（仮）は Argante 製のローグライクカードゲーム。
prototype/index.html を開くだけで動く。
ビルドツールを前提にせず、file:// 対応を守る。
JavaScript はグローバル関数と index.html の読み込み順で動く。
ユーザーは高速な試作検証を重視する。
完全な一般化より、短時間で安全に試せる状態を優先する。

このファイルは常時守る規則だけを置く。
詳細仕様、調査記録、過去の経緯は docs 内へ分離している。
作業開始時はこのファイルを最後まで読む。
その後、作業内容に対応する資料だけを読む。
履歴を最初から全部読まない。
既存挙動の理由が不明な時は docs/HISTORY.md を検索する。

---

## 1. 最優先ルール

オンライン対戦（PvP）と通常プレイ（PvE）を一致させる。
カード、指輪、アイテム、戦闘ルールは原則として両方に反映する。
同じルールを PvE とオンラインへ別々に書かない。
修正を2箇所へ複製するのではなく、実装を1箇所へ集約する。

戦闘結果の唯一の実装は prototype/js/battle/core.js。
魔導板から出撃編成への変換は prototype/js/battle/formation.js。
演出順序と見せ方の規則は prototype/js/battle/present.js。
イベント1件の演出は prototype/js/battle/present_events.js。
共通のDOM描画は prototype/js/engine/render.js。
PvEの受け口は prototype/js/engine/battle.js。
オンラインの受け口は prototype/js/online/board.js。

描画の受け口に戦闘ルールや数値計算を書かない。
受け口はコアのイベントを表示するだけにする。
present.js では G、DOM、数値計算に触れない。
core.js では DOM、G、Math.random を使わない。
乱数は引数の rng を使う。

if (isOnline) や if (G.isVersus) で別実装を作らない。
PvEだけ、オンラインだけという利用者指定は勝手に両側へ広げない。
片側限定機能は後から共通化できる形にする。
片側限定の理由は docs/ONLINE.md の登録表へ追記する。

コアへ渡すデータの作り方も共通化する。
同じコアを呼んでいても入力が違えば結果は一致しない。
編成、指輪、基礎能力、強化状態、逃走状態などを送信時に落とさない。
オンライン変更では payload と receiver の検査を必ず行う。

資源変更はコアから PvE の G へ書き戻す。
ライフ、マナ、ゴールドの変更経路を新設した時は出口まで確認する。
イベントを出すだけ、state を変えるだけで終わらせない。

---

## 2. キーワードとカード名

カード名や強化カード名をキーワードとして扱わない。
カード名を効果文のキーワード位置へ書かない。
キーワードはシートのキーワード定義にあるものだけ。

効果文を持つ強化カード名は効果の識別子であり、キーワードではない。
効果文が空の強化カードだけが本物のキーワードになり得る。
一覧の唯一の実装は core.js の CORE_KEYWORD_CARD_NAMES と CORE_EFFECT_CARD_NAMES。

新しい強化カードを追加したら CORE_EFFECT_CARD_NAMES を更新する。
unit.keywords から消すと効果まで消えるカードがある。
削除用一覧とカウント除外用一覧を混同しない。
カード追加・変更時はカード名がキーワード表示へ混ざらないことを確認する。

カード、指輪、アイテムの数値や文言はシートを正とする。
効果文の数値をコードへ重複して書かない。
画面文言をコードへ戻さない。
データ変更時は docs/DATA_TEXT.md を読む。

---

## 3. 実装境界

新しい戦闘効果はまず core.js に書く。
runBattleCore から同じ処理が呼ばれることを確認する。
演出が必要ならコアからイベントを出す。
イベントの見せ方は present_events.js または render.js に1回だけ書く。
PvEとオンラインの受け口は共通関数を呼ぶだけにする。

既存イベントで表現できる効果に新しいイベント種別を増やさない。
新しいイベント種別を足したら両方の受け口を追加する。
オンライン receiver の追加漏れを検査する。
新しい固有VFXには演出パリティのシナリオを追加する。

複数対象ダメージは1体ずつ applyHit しない。
全員へ入れてからまとめて誘発する共通処理を使う。
ダメージ種別と batch をコアで確定する。
再生側でイベント列から束を推測しない。

効果の演出は直列に見せる。
前のVFXが消える前に次の異なる効果を重ねない。
同じ効果の同時対象は同じ波として扱う。
キャラクター単位ではなく効果単位で区切る。

盤面配列の意味を PvE とオンラインで変えない。
前衛、後衛、左右の並びを共通関数で決める。
召喚位置や詰め直しを描画側で再計算しない。

古い経路を残したまま新しい経路を足さない。
二重実装を作らない。
互換処理を削る時は到達経路とセーブデータを確認する。
整理のついでに仕様や見た目を変えない。

---

## 4. file:// とグローバル構成

ES Modules へ移行しない。
バンドラを導入しない。
import / export 前提にしない。
ローカルサーバーだけで動く実装にしない。

ファイルを分割してもグローバル関数のままにする。
index.html の script 読み込み順を守る。
依存する関数より前に定義または読み込みされることを確認する。
新しいJSを追加したら index.html に読み込みを追加する。

変更したJSの script src の ?v= を上げる。
シート由来データを更新したら local_xlsx_data.js の ?v= も上げる。
SVGや画像のキャッシュ問題がある時は資産URLの版も検討する。
版更新だけで不具合を隠さない。

prototype/index.html は大きい。
CSSの後勝ちと !important の競合を前提に調べる。
詳細度を上げる前に、後方の競合規則を探す。
同じプロパティの重複規則を増やさない。

---

## 5. データとシート

マスターの Vesselbound_data.xlsx は利用者管理。
編集、移動、削除をしない。
反映は prototype/tools/update_local_xlsx_data.py を使う。
更新対象のキーだけを指定する。

Node側の検査は local_xlsx_data.js を読む。
ブラウザ側の検査は xlsx を直接読むものがある。
file:// 実機は local_xlsx_data.js を読む。
データ源の違いを忘れない。

シートの列はヘッダ名で参照する。
列番号や固定位置で参照しない。
列追加で別の値を読む事故を防ぐ。
balance_sim では sheet_data.js を経由する。

シートに列や値を足したら内蔵CSVを再生成する。
監査が通っても実機で効かない時は内蔵CSVの古さを疑う。
画面文言の例外一覧は docs/DATA_TEXT.md で管理する。

---

## 6. UI・CSS

UI変更では見た目の数値を整理のついでに変えない。
座標、寸法、角丸、発光、尺を勝手に調整しない。
利用者が基準に指定した要素を変更しない。
基準へ他の要素を合わせる。

枠、画像、黒背景、発光はレイヤーを分ける。
発光対象のレイヤーだけへ filter を付ける。
親要素の filter は全子孫を合成して光らせることに注意する。
内側を光らせない指示がある時は枠レイヤーを独立させる。

疑似要素へ広い filter:none を当てない。
子孫一括の filter:none は状態クラスを消すことがある。
クラスが付いているのに見た目が変わらない時は後勝ちを調べる。
! important の積み増しだけで押さえ込まない。

ドラッグ元、ドラッグゴースト、移動先を別状態として確認する。
body直下のゴーストは game-scale の外にある。
枠線、文字、寸法の実画面値を補正する。
ドラッグ中も残すべき背景や枠を消さない。

ボタンの発光はボタンの実形状へ合わせる。
四角い影をボタン画像の下に作らない。
発光用疑似要素に不透明な矩形背景を残さない。
同様の再発防止事項は docs/UI_CSS.md に記録する。

UIやCSSへ触る前に docs/UI_CSS.md を読む。
音やVFXへ触る前に docs/PRESENTATION.md を読む。
音の詳細は docs/SOUND_EFFECT_RULES.md も読む。

---

## 7. セーブと状態

セーブ形式を変える時は docs/SAVE.md を読む。
保存対象と復元対象を対で変更する。
新規フィールドは古いセーブで欠けても動く既定値を持たせる。
一時DOM状態や演出中の参照を保存しない。

performance.now() の値を保存しない。
セッションを跨ぐ時間は経過量または Date.now() 基準で扱う。
乱数の鍵に再試行回数が必要な機能では省略しない。
復元後に同じ結果を再生できることを確認する。

G のフィールド追加時は保存、復元、初期化を確認する。
戦闘だけの一時状態を恒久保存へ混ぜない。
オンラインサーバーが持つ状態とローカル保存を混同しない。

---

## 8. 作業開始時

まず git status と git diff を確認する。
利用者や別作業の変更を自分の変更とみなさない。
未コミット変更を消さない。
関係ない差分を整形しない。

AGENTS.md を最後まで読む。
次の参照表から必要な資料を選ぶ。
選んだ資料は作業前に読む。
不明な履歴は docs/HISTORY.md でキーワード検索する。

対象コードを rg で検索する。
定義、呼び出し元、受け口、データ源を確認する。
同名の旧実装が残っていないか確認する。
CSSは同じ要素の後方規則まで検索する。

実装前に影響範囲を決める。
PvE、オンライン、セーブ、演出、UI、データのどれに触れるか判断する。
触る範囲に対応する検査を決める。
大きな変更は小さな単位へ分ける。

利用者が診断だけを求めた時は勝手に修正しない。
修正を求められた時は安全な範囲で最後まで実装する。
権限や選択が必要な時だけ質問する。
推測で別仕様へ広げない。

---

## 9. 作業中

短い途中経過を日本語で報告する。
ツールを使う前に何を確認するか伝える。
長時間無言にしない。
同じ進捗を繰り返し報告しない。

既存の変更を保持する。
自分が触る箇所と重なる時は差分を読んでから編集する。
機械的な大規模置換は対象を限定する。
生成物や一時ファイルをプロジェクトへ残さない。

ファイル編集は apply_patch を使う。
検索は rg または rg --files を優先する。
破壊的なGit操作をしない。
git reset --hard と git checkout -- を使わない。
利用者の依頼なしにコミットしない。

リファクタリングと機能変更を混ぜない。
CSS整理と見た目変更を混ぜない。
ファイル分割と挙動変更を混ぜない。
原因を切り分けられる差分にする。

実装中に既存の不具合を見つけても勝手に範囲を広げない。
今回の変更に不可欠なら理由を報告する。
不可欠でなければ docs/HISTORY.md の未解決事項と重複するか確認する。
新たに記録する場合は再現条件と対象を具体的に書く。

---

## 10. 終了前の最低確認

何を触っても変更した全JSへ node --check を行う。
git diff --check を行う。
index.html の該当 ?v= を上げる。
不要なデバッグコードや一時ログが無いことを確認する。

効果、キーワード、カードデータへ触ったら balance_sim 5本を通す。
演出、VFX、SE、数値表示へ触ったら anim_check と present_parity を通す。
オンライン編成や送受信へ触ったら online_payload と online_receivers を通す。
オンライン経路全体へ触ったら online_pipeline を通す。
戦闘進行へ触ったら loop_parity を通す。
見た目だけなら anim_check を通す。
セーブへ触ったらセーブ検査3本を1本ずつ通す。

検査名と実行方法の詳細は docs/TESTING.md を読む。
ローカルサーバーはポート5500を使う。
残留Chromeや前の検査プロセスがポートを掴んでいないか確認する。
セーブ検査は続けて起動せず、1本ずつ待つ。

自動テストの通過を実機確認と書かない。
画面を見ていない場合は未確認と明記する。
見た目の印象や実際の音を検査だけで断定しない。
利用者が実測を求めた時、または原因を絞れない時はヘッドレスChromeを使う。

不具合を直したと報告する前に再現条件で確認する。
計算済みスタイルだけでなく必要なら画像も確認する。
PvEとオンラインの双方が対象なら両方を確認する。
失敗した検査を隠さない。

---

## 11. 完了報告

最初に結果を書く。
変更した内容を簡潔に書く。
確認した項目と未確認の項目を分ける。
調整可能な定数がある場合は名前と現在値を書く。

テストを通しただけなら、その事実を報告する。
実機で見た場合だけ実機確認済みと書く。
ブラウザ画像を確認した場合は画像確認済みと書く。
実際の音を聞いていない場合は聴感未確認と書く。

未解決事項を完了扱いにしない。
途中までの変更が残る場合は対象ファイルと残作業を書く。
利用者が次に確認すべき点がある場合だけ伝える。
長い作業記録を最終回答へ再掲しない。

---

## 12. 参照資料の選び方

戦闘効果、対象選択、死亡、召喚、能力値:
docs/BATTLE.md

演出、VFX、SE、ダメージ表示、タイミング:
docs/PRESENTATION.md

UI、CSS、枠、発光、ドラッグ、レイアウト:
docs/UI_CSS.md

検査コマンド、ヘッドレスChrome、検査の落とし穴:
docs/TESTING.md

シート、カードデータ、効果文、画面文言、内蔵CSV:
docs/DATA_TEXT.md

セーブ、復元、プレイ時間、再開:
docs/SAVE.md

オンライン送受信、PvE/PvP共通化、片側限定:
docs/ONLINE.md

ファイル構成、G、作業手順、リファクタリング:
docs/PROJECT_MAP.md

既知の問題、完了済み変更、過去の原因、利用者指定:
docs/HISTORY.md

ゲームシステム全般の補足:
docs/GAME_SYSTEMS.md

効果音の個別ルール:
docs/SOUND_EFFECT_RULES.md

複数分野に触れる場合は必要な資料を複数読む。
資料間で矛盾した場合は、この AGENTS.md を優先する。
次に利用者の最新指示を優先する。
古い docs/HISTORY.md の記録を最新指示より優先しない。

---

## 13. 変更種類別チェックリスト

### カードや効果

docs/BATTLE.md と docs/DATA_TEXT.md を読む。
docs/ONLINE.md の共通化規則を確認する。
カード名をキーワードへ入れない。
コアの1箇所へ実装する。
runBattleCore から呼ばれることを確認する。
必要なイベントだけを出す。
balance_sim 5本を通す。
オンライン受け口を確認する。
?v= を上げる。

### 演出や音

docs/PRESENTATION.md を読む。
必要なら docs/SOUND_EFFECT_RULES.md も読む。
規則を present.js に置く。
1件の表示を present_events.js に置く。
両受け口から同じ関数を呼ぶ。
固有VFXシナリオを追加する。
present_parity と anim_check を通す。
実際の音は聴感未確認と区別する。

### UIやCSS

docs/UI_CSS.md を読む。
基準となる要素を特定する。
後方の競合規則を検索する。
レイヤー構造を確認する。
通常、ホバー、押下、ドラッグ元、ゴースト、移動先を確認する。
空枠と中身ありを確認する。
不足、売切、無効状態を確認する。
計算済みスタイルと画像を必要に応じて比べる。
anim_check を通す。

### オンライン

docs/ONLINE.md を読む。
共通コアへ渡す全フィールドを確認する。
formation の共通関数を使う。
片側分岐を作らない。
online_payload を通す。
online_receivers を通す。
必要なら online_pipeline と loop_parity を通す。

### セーブ

docs/SAVE.md を読む。
保存と復元を対で変更する。
古いセーブの既定値を用意する。
一時状態を保存しない。
セーブ検査3本を1本ずつ通す。
再読み込み後の状態を確認する。

### データや文言

docs/DATA_TEXT.md を読む。
xlsxを直接編集しない。
ヘッダ名で列を読む。
内蔵CSVを更新する。
Node検査とブラウザ検査のデータ源を区別する。
local_xlsx_data.js の ?v= を上げる。

### リファクタリング

docs/PROJECT_MAP.md を読む。
利用者が整理を依頼した時だけ行う。
機能変更と混ぜない。
1ファイルまたは1責務ずつ進める。
グローバル構成と file:// を守る。
前後の計算済みスタイルや挙動を比較する。
削除したものと移動したものを報告する。

---

## 14. 履歴の扱い

完了済みの長い調査記録は AGENTS.md へ戻さない。
再発防止として常時必要な規則だけをここへ追記する。
個別不具合の原因と修正履歴は docs/HISTORY.md へ追記する。
UI固有の再発防止は docs/UI_CSS.md へ追記する。
戦闘固有の詳細規則は docs/BATTLE.md へ追記する。
演出固有の詳細規則は docs/PRESENTATION.md へ追記する。

新しい資料を増やす前に既存9資料へ収まるか確認する。
資料の冒頭に「いつ読むか」を書く。
AGENTS.md の参照表も更新する。
同じ詳細説明を複数資料へコピーしない。
リンク先ファイル名を変更したら参照を一括確認する。

未解決事項には再現条件、対象、現状、勝手に直さない条件を書く。
完了事項には日付、原因、採用した唯一の実装を書く。
一時的な検査結果を常時ルールにしない。
利用者の最新指示で覆った古い仕様は履歴として明示する。

---

## 15. 最終確認

今回の依頼範囲を満たしている。
関係ない変更をしていない。
PvEとオンラインの扱いを確認した。
データ源を確認した。
変更したJSの構文を確認した。
必要な検査を実行した。
?v= を更新した。
git diff --check を通した。
一時ファイルを残していない。
未確認事項を明記した。
日本語で報告する。
